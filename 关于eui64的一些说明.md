# 关于 EUI-64 的一些说明

## 1. 什么是 EUI-64

EUI-64 是 IPv6 里生成**接口标识符**（地址的后 64 位）的一种方式。EUI 是 "Extended Unique Identifier"（扩展唯一标识符）的缩写，64 表示它是 64 位。

EUI-64 由网卡的 MAC 地址（48 位）转换而来：在 MAC 地址的第 3、4 字节之间插入固定的填充位 `FF FE`，再把**第 1 个字节的 U/L 位取反**，就得到 64 位的接口标识符。

- 因为 MAC 地址唯一，转换出的接口标识符也唯一；
- 反过来也能从 IPv6 地址推回 MAC，便于网络管理和故障排查（这既是优点也是隐私缺点，所以现在不少系统改用随机后缀）。

## 2. 怎么判断一个 IPv6 地址是不是 EUI-64 生成的

不用死记位序号，**按字节拼**最不容易算错：

> 接口标识符 = `MAC[0] ⊕ 0x02` + `MAC[1]` + `MAC[2]` + `FF` + `FE` + `MAC[3]` + `MAC[4]` + `MAC[5]`

换成好记的说法：**把 64 位接口标识符的中间两个字节读成 `FFFE`**（地址形如 `xxxx:xxFF:FExx:xxxx`），同时第 1 个字节和真实 MAC 的首字节只差一位，那它就是 EUI-64 后缀。

举例，MAC 地址是 `79:F3:AE:E0:76:82`：

| 步骤 | 结果 |
|---|---|
| 第 1 字节取反 U/L 位（`79` = `0111 1001` → `0111 1011`） | `7B` |
| 拼上 MAC 前 3 字节（首字节已取反） | `7B F3 AE` |
| 插入填充位 | `FF FE` |
| 拼上 MAC 后 3 字节 | `E0 76 82` |
| 得到 64 位接口标识符 | `7BF3:AEFF:FEE0:7682` |

把它接到运营商给的前缀后面，完整地址就是：

```
2408:1145:1419:1981:7BF3:AEFF:FEE0:7682
                   └─── 7BF3:AEFF:FEE0:7682 由 MAC 转换而来 ───┘
```

也就是文档里常写的 `你的前缀:7BF3:AEFF:FEE0:7682`。

> [!TIP]
> 关于"取反的是哪一位"：MAC 首字节里被翻转的是 **`0x02` 那一位**（`79 → 7B`）。
> 有的资料按"从低位往高位数第 2 位"描述，有的按"从高位往低位数第 7 位"描述，指的是同一位，看到时不用怀疑自己算错了。

## 3. 前缀是哪来的、在路由器上怎么配

EUI-64 只决定地址的**后 64 位**；前 64 位的**前缀不是自己生成的**，而是上级"发"的：

- 家宽由运营商通过 PPPoE / DHCPv6-PD 下发一段前缀（常见 `/56`、`/60`、`/64`）；
- 光猫桥接 + 路由器拨号时，路由器在 `wan6`（或拨号接口）上拿到这段前缀，再按 `IPv6 分配长度` 往下分给 `lan`。

路由器上相关的几项（LuCI 路径以 `网络 → 接口` 为例）：

| 位置 | 项 | 作用 |
|---|---|---|
| `wan6` / 拨号接口 | 请求 IPv6 前缀长度 | 向运营商要一段前缀（例：`/60`） |
| `lan` → 高级设置 | `IPv6 分配长度` | 从拿到的前缀里切多长给 lan（例：`64`） |
| `lan` → 高级设置 | `IPv6 分配提示` | 指定切出哪一段（例：`0`；留空为自动） |
| `lan` → 高级设置 | **`IPv6 后缀 = eui64`** | 让下游客户端用 EUI-64 生成**固定不变**的后缀 |
| `lan` → DHCP 服务器 → IPv6 设置 / IPv6 RA 设置 | `RA 服务`、`DHCPv6 服务`、`启用 SLAAC` | 决定客户端怎么拿到地址（无状态 / 有状态） |

其中「**IPv6 后缀 = eui64**」是关键一项：设了它，即使运营商那边的前缀变了，客户端地址的**后缀仍是 `MAC → EUI-64` 算出来的固定值**。对应的 uci 配置是：

```sh
uci set network.lan.ip6assign='64'      # IPv6 分配长度
uci set network.lan.ip6hint='0'         # IPv6 分配提示（可选）
uci set network.lan.ip6ifaceid='eui64'  # IPv6 后缀 = eui64
uci commit network
```

> [!NOTE]
> 所以"前缀怎么配置"的答案是：**前缀不用你配，你能决定的是怎么分配它、以及客户端的后缀是否固定**。前缀变了也不用手动改任何东西 —— 前提是防火墙上那条带掩码的规则写对了（下一节）。

## 4. 为什么用 EUI-64 后缀做端口转发

国内家宽的 IPv6 **前缀是动态的**，重拨一次可能就变，因此不能像 IPv4 那样把设备地址写死。

用 EUI-64 的地址做转发，好处是**前缀会变、后缀不变**：在 `网络 → 防火墙 → 通信规则` 里添加转发规则时，"目标地址"写成"前缀任意 + 后缀固定"的形式，用掩码把后 64 位锁住：

```
目标地址：::216:3eff:fe89:9e30/-64
                    └ 掩码 -64：只匹配后 64 位
```

这条规则的意思是：**任意前缀** + `216:3eff:fe89:9e30` 这个后缀的流量都命中（端口可选，要限端口就填在"目标端口"）。前缀变了也不用改规则。

> [!TIP]
> 有些教程把这个掩码叫"反向掩码"，其实它和我们平时说的反掩码（wildcard mask）不是一回事，这里就是普通的"地址/掩码"写法。

另外，因为后缀与 MAC 一一对应，你的 DDNS 脚本也可以**直接用设备的 MAC 算出它的 IPv6 地址**给域名做解析，不必等邻居表刷新。
