# 小白问题速查

此处收集群友能独立自主解决的问题。问问题之前先一看，防止问题过于抽象被 t。

> [!NOTE]
> 本文涉及命令的地方请注意版本差异：**24.10 及更早用 `opkg`，25.12（含 master 快照）改成了 `apk`**，详见 [README](./README.md) 的「版本与命令对照」。

## 自编译的固件无法进入网页（LuCI）

**解决办法**：进入 `make menuconfig`，选中 `LuCI → Collections → default-settings-chn`（或 `default-settings`，英文界面）即可。

**原因**：默认的 master 分支不带网页（LuCI）。

如果你已经刷好固件、不想重新编译：

```sh
# 24.10 及更早（opkg）
opkg update && opkg install default-settings-chn
# 25.12 / 快照（apk）
apk update && apk add default-settings-chn
```

> [!NOTE]
> 官方发布的固件**已经内置**了 `default-settings` 与 `default-settings-chn`，只有自编译 / 自定义构建时才需要自己勾选这一项。
>
> If you are a native English speaker, select `default-settings`.
>
> If you are a user of other languages (supported by this project), please select your language in `LuCI → 2. Modules → Translations`.

## 使用 selector 构建带最新内核的固件

* 前置要求
  * 所使用的插件必须全部基于 `nftables`（常见的 docker、ssrp、turbo-acc 等**不兼容**）
* 操作步骤
  * 打开 [firmware selector](https://firmware-selector.immortalwrt.org/)，把版本从 `24.10.x` / `25.12.x` 切换为 `SNAPSHOT`
  * 输入你的设备型号以查找对应固件
  * 选择 "Customize installed packages and/or first boot script"（自定义预安装软件包和首次启动配置脚本）
  * 在预安装的软件包内填入 `default-settings-chn`
  * 点击**请求构建**
  * 构建完成后下载刷入

> [!TIP]
> 把当前稳定版（如 24.10）的软件包清单复制到快照构建里一般也可以，但**只复制包名**：稳定版的安装包是 `.ipk`，快照是 `.apk`，两种格式不通用。

## 每次编译前应该做什么

### 固定动作

```sh
git pull
./scripts/feeds update -a
./scripts/feeds install -a
make defconfig
make download -j32
```

> [!TIP]
> 执行时，窗口内出现 `make[1] tools/download` 字样即可新开一个窗口开始编译（**不要中断**当前窗口）。
>
> 命令里的 `-j32` 是按 CPU 线程数来的，按自己的机器调整（`nproc` 可以查看线程数）。

## 初次编译不要带过多插件

只勾选 `default-settings-chn` 即可。如果你选中的插件依赖 `rust` 或其他语言的工具链，编译时间极大概率会翻倍，而且会引入更多不确定性。

> [!TIP]
> 先把基底打好，再想其他的。

## 遇到因插件编译出错而 "ERROR 2" 的解决办法

重新 `make` 时减少编译线程，以便更准确地定位报错：

```sh
make -j1 V=s
```

再删除出错的软件包源码，重新构建：

```sh
rm -rf dl/出错的软件包名*
```

<!--
> [!TIP]
> 当你见到 `dl/uhttpd-2023.06.25~34a8a74d.tar.zst` 这种奇怪的源码包，删除之，继续 make 即可
-->

## 重新编译某个包

使用下面的命令重新构建（`busybox` ~~（boombox）~~ 可通过此方式解决）：

```sh
make {toolchain,target,package/软件包名}/compile V=s -j17
```

## 使用本地 LLVM（clang）

```
# CONFIG_BPF_TOOLCHAIN_BUILD_LLVM is not set
CONFIG_BPF_TOOLCHAIN_HOST=y
CONFIG_BPF_TOOLCHAIN_HOST_PATH=""
```

在配置菜单中位置为 `Advanced configuration options (for developers) → BPF toolchain (Use host LLVM toolchain)`，默认为空的参数就是 clang 路径。

## 自编译固件如何开热点

`Network → WirelessAPD`：

`hostapd-openssl` 和 `hostapd-mbedtls` 二选一，保存即可。

## 勾选无线网卡驱动（以 mt7922 和 mt7921 为例）

`Kernel modules → Wireless Drivers`，勾选 `kmod-mt7921e` 即可。

mediatek 无线网卡的后缀规则：

| 后缀 | 含义 |
|---|---|
| e | PCIE |
| u | USB |
| s | SDIO |

## 通过手机或"随身 wifi 棒子"类产品用 USB 接入路由器上网

`Kernel modules → USB Support`：`kmod-usb-net-rndis`、`kmod-usb-net-ipheth`

`Utilities`：`usbutils`、`usb-modeswitch`

* 在 LuCI 里到 `网络 → 接口 → 设备`，寻找 `usb*` 字样的设备
* 前往 `网络 → 接口`，新建接口，配置名称，协议选择为 **DHCP 客户端**，设备选择为 `usb*`（`*` 是数字，一般是 0）
* 重复上一步，把协议改为 **DHCPv6 客户端**（实现 **双栈**）
* 配置防火墙（一般加入 `wan` 区域）

开启 USB 共享网络后，SSH 或以其他方式连接路由器的 shell，执行：

```sh
ifconfig    # 查看效果
```

或者直接在 LuCI 的接口页面里看。

> [!TIP]
> rndis 虽然相对"老旧"，但是还在用。
>
> LuCI 内需查找 `网络 → 接口 → 设备`，寻找 `usb*` 字样的设备进行配置。
>
> cdc-* 模式的设备（如 ZTE F50）会映射为 `eth*`。

## 开启 UASP

`Kernel modules → USB Support`：`kmod-usb-storage-extras`、`kmod-usb-storage-uas`

`Utilities`：`usbutils`、`usb-modeswitch`

接入设备后，SSH 或以其他方式连接路由器的 shell，执行：

```sh
lsusb -t    # 以查看设备情况
```

## nvme 硬盘支持

`Kernel modules → Block Devices → kmod-nvme`

> [!NOTE]
> 以上 `kmod-*` 包都在官方仓库的 kmods 源里，刷好固件后也可以在线安装（`opkg install` / `apk add`）。注意 `kmod-*` 与内核版本绑定，升级固件后需要重新安装。

## 常见 LuCI 插件的平替方案（fw3 → fw4）

| 旧方案 | 新方案 |
|---|---|
| `ssrp` | `homeproxy` |
| `syncdial` + `mwan3` | `pbr` |
